export interface ResponseDTO {
    success: boolean
    code: number
    message: string
    data?: any
}

export interface ResponseData {
    success: boolean
    code: number
    message: string
    data?: any
}